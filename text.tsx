const solve = () => {
    //수도 코드:
    
    // getBusPosList.length -1 번 반복하면 나온다.
    // stationList[ord]; ord 정거장의 1번째 2번쨰 시간 알 수 있음

    // currOrd = endOrd

    // posIndex = busPosList.length-1

    // for(i=0; i<busPosList.length -1; i++)
        // posIndex -= 1
        // curr2th = busPosList[posIndex]
        // curr2thTime = stationList[currOrd].2thTime

        // sumTime += curr2thTime
        // currOrd = curr2th.ord+1;

        // curr1th = curr2th
        // curr1thTime = stationList[currOrd].1thTime
        // sumTime -= curr1thTime

        // -------- curr0 ~ curr1까지 시간 구함.

    // userBusToCurrordTime = curr2thTime = stationList[currOrd].2thTime
    // sumTime += curr2thTime

    
}

solve();